services:
  fastleaderboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastleaderboard-backend
    ports:
      - "8080:8080"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - PORT=8080
      - THREADS=4
    restart: unless-stopped
